language: python

python:
  - "2.7"
  - "3.4"

before_install:
  - echo $TRAVIS_BUILD_DIR

addons:
  apt:
    packages:
    - libblas-dev
    - liblapack-dev
    - libatlas-base-dev
    - gfortran

install: 
  - python setup.py install

script: py.test